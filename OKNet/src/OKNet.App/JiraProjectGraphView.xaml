<UserControl x:Class="OKNet.App.JiraProjectGraphView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:jira="clr-namespace:OKNet.App.UserControl.Jira"
    xmlns:app="clr-namespace:OKNet.App"
    mc:Ignorable="d"
    d:DesignWidth="{Binding Width}" Height="{Binding GraphHeight}"
    d:DataContext="{d:DesignInstance app:DesignJiraInProgressGraphViewModel, IsDesignTimeCreatable=True}" Background="White">
    <Grid Height="{Binding GraphHeight}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="10*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <ItemsControl ItemsSource="{Binding Ticks}" Height="{Binding GetGraphHeight}" Margin="{Binding Margin}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical"></StackPanel>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Height="{Binding Height}" Text="{Binding DisplayText}" Margin="0"></TextBlock>
                        <Separator Grid.Column="1" VerticalAlignment="Top" Margin="0"></Separator>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1">

        </Border>
        <ItemsControl Grid.Column="1" ItemsSource="{Binding ProjectIssuesCount}" Height="{Binding GetGraphHeight}" Margin="{Binding Margin}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"></StackPanel>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <jira:JiraProjectStatusGraphItem DataContext="{Binding}" Margin="1,0,1,0"></jira:JiraProjectStatusGraphItem>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>